# run-frontend
Start the Vite dev server on port 5173

1. Install deps: `cd frontend && npm install`
2. Start dev server: `npm run dev`
3. App will open at `http://localhost:5173`
4. Backend must be running at `http://localhost:8001`
